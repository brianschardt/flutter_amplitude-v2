name: flutter_amplitude_v2
description: Flutter pluging for Amplitude SDK
version: 0.1.1
homepage: https://github.com/brianschardt/flutter_amplitude-v2

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    androidPackage: ly.eval.flutteramplitude
    pluginClass: FlutterAmplitudePlugin
